# WiFi 管理器使用指南

## 📡 功能說明

內建的 WiFi 管理器提供觸控友好的介面，方便在樹莓派上管理 WiFi 連線。

## 🎯 開啟方式

### 在儀表板中開啟
- **鍵盤**: 按 `F12` 或 `Ctrl+W`
- **觸控**: 長按狀態欄 3 秒（待實作）

### 獨立運行
```bash
python3 wifi_manager.py
```

## ✨ 功能特點

### 1. 掃描網路
- ✅ 自動掃描可用 WiFi
- ✅ 顯示信號強度
- ✅ 標示加密網路 🔒
- ✅ 按信號強度排序

### 2. 連線管理
- ✅ 觸控友好的密碼輸入
- ✅ 顯示/隱藏密碼選項
- ✅ 記住網路設定
- ✅ 即時連線狀態顯示

### 3. 自動化
- ✅ 自動檢查連線狀態（每5秒）
- ✅ 連線失敗自動提示
- ✅ 支援開放網路和加密網路

## 🔧 系統需求

### NetworkManager
WiFi 管理器使用 `nmcli` 命令，需要安裝 NetworkManager：

```bash
sudo apt-get install network-manager
```

### 啟用 NetworkManager
```bash
sudo systemctl enable NetworkManager
sudo systemctl start NetworkManager
```

### 賦予使用者權限
```bash
# 將使用者加入 netdev 群組
sudo usermod -aG netdev $USER

# 重新登入生效
```

## 📱 觸控操作

### 密碼輸入
- 使用虛擬鍵盤（系統自動調用）
- 支援顯示/隱藏密碼
- Enter 鍵快速連線

### 網路列表
- 點擊選擇網路
- 點擊「連線」按鈕連線
- 下拉可以重新掃描

## 🎨 界面說明

### 信號圖示
- `▂` 弱訊號 (0-25%)
- `▂▄` 中弱訊號 (25-50%)
- `▂▄▆` 中強訊號 (50-75%)
- `▂▄▆█` 強訊號 (75-100%)

### 網路圖示
- 🔒 加密網路（需要密碼）
- 📶 開放網路（無需密碼）

### 狀態顏色
- 🟢 綠色: 已連線
- 🔴 紅色: 未連線/連線失敗
- 🟡 黃色: 連線中

## 🔒 安全性

### 密碼儲存
- 密碼由 NetworkManager 加密儲存
- 儲存在 `/etc/NetworkManager/system-connections/`
- 使用系統級權限保護

### 網路設定
```bash
# 查看已儲存的網路
nmcli connection show

# 刪除已儲存的網路
sudo nmcli connection delete "網路名稱"

# 查看當前連線
nmcli device status
```

## 🐛 故障排除

### 無法掃描網路
```bash
# 檢查 WiFi 硬體狀態
nmcli radio wifi

# 如果關閉，開啟 WiFi
nmcli radio wifi on

# 重啟 NetworkManager
sudo systemctl restart NetworkManager
```

### 無法連線
```bash
# 檢查 WiFi 介面
ip link show

# 手動掃描測試
nmcli dev wifi list

# 查看詳細錯誤
journalctl -u NetworkManager -f
```

### 權限問題
```bash
# 檢查是否在 netdev 群組
groups

# 如果沒有，加入群組
sudo usermod -aG netdev $USER
# 重新登入
```

## 🚀 開機自動連線

NetworkManager 會自動連線到已記住的網路。

### 設定優先順序
```bash
# 查看連線列表
nmcli connection show

# 設定優先順序 (數字越大優先級越高)
nmcli connection modify "網路名稱" connection.autoconnect-priority 10
```

## 💡 進階功能

### 隱藏網路連線
如果需要連線到隱藏的 SSID：

```bash
nmcli dev wifi connect "隱藏SSID" password "密碼" hidden yes
```

### 固定 IP
```bash
nmcli connection modify "網路名稱" \
  ipv4.method manual \
  ipv4.addresses 192.168.1.100/24 \
  ipv4.gateway 192.168.1.1 \
  ipv4.dns "8.8.8.8 8.8.4.4"
```

### 熱點模式
```bash
nmcli dev wifi hotspot ssid "我的熱點" password "密碼"
```

## 🎯 整合到儀表板

WiFi 管理器已整合到主儀表板中：

1. 按 `F12` 開啟 WiFi 設定
2. 選擇網路並連線
3. 連線成功後自動關閉
4. 儀表板繼續正常運作

## 📝 命令列快速參考

```bash
# 掃描 WiFi
nmcli dev wifi list

# 連線到網路
nmcli dev wifi connect "SSID" password "密碼"

# 斷開連線
nmcli dev disconnect wlan0

# 查看連線狀態
nmcli connection show --active

# 刪除已儲存的連線
nmcli connection delete "SSID"
```
